{{ form_start(form) }}
<div class="container mt-4 text-center">
    <div class="col-md-4">{{ form_row(form.statute) }}


{{ form_rest(form) }}
<button class="btn btn-dark">{{ button| default('Enregistrer') }}</button>
{{ form_end(form) }}
    </div>
</div>